name: CI (Go)

on: pull_request

permissions:
  contents: read

concurrency:
  group: ci-go-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read
