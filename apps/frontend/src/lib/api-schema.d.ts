/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Liveness probe. Touches nothing — not even the database pool. */
        get: operations["healthz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readyz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Readiness probe. Reads the installation singleton, which both proves the database is reachable and carries the retention job's last run. */
        get: operations["readyz"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/setup/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** First-run state. Public and deliberately says nothing about who the owner is — the installation's contact address is not a secret worth publishing, and a visitor only needs to know whether the setup screen should open at all. */
        get: operations["getSetupStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/setup/complete": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Claim the installation: create the owner account, their first household, and lock setup. Rate limited (5 per 15 minutes) because the body carries SETUP_SECRET. */
        post: operations["completeSetup"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/invitations/lookup": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** What an invitation link points at, for the invite page. Public and rate limited (20 per 10 minutes): together with accept, this is the one unauthenticated path that reveals whether a token exists. */
        get: operations["lookupInvitation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/invitations/accept": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Accept an invitation, as the signed-in user or by creating the invited account. Rate limited with the lookup above. */
        post: operations["acceptInvitation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/households/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Every household the caller belongs to, for the household switcher. Ordered by lower-cased display name so the list does not reshuffle when somebody renames a household with a capital letter. */
        get: operations["listMyHouseholds"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/households": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create a household with the caller as its owner. Restricted (REF §A2): the installation owner may always; anybody else only while they belong to no household at all, so a member cannot mint extra households or squat inbound addresses. Rate limited (10 per hour). */
        post: operations["createHousehold"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/households/{slug}/leave": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Give up your own membership of this household. Refused for the last owner, which is what keeps a household from being left with nobody who can administer it. */
        post: operations["leaveHousehold"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/inbox/{slug}/providers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The inbox landing page: one tile per provider the caller may read, with its message counts and a preview of the newest message.
         *     A member route rather than an owner one, with the per-provider check INSIDE it: an owner sees every provider in the household, a member only the ones they have been granted. That is the whole point of provider access — the household's mail is split per mailbox, not per role.
         */
        get: operations["listInboxProviders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/inbox/{slug}/providers/{providerKey}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One provider's inbox, newest first, one keyset page at a time.
         *     The 403 for a member without access to this provider is answered BEFORE the provider is looked up, so a member cannot use the difference between 403 and 404 to enumerate which providers the household has.
         */
        get: operations["listProviderMessages"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/inbox/{slug}/messages/{messageId}/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Mark one message new, used or expired — the "I have used this code" button.
         *     The message is looked up before the body is validated, deliberately: a status the server does not know, sent for a message that is not this household's, is answered 404 rather than 400, so the route never confirms the existence of a message the caller may not read.
         */
        patch: operations["updateMessageStatus"];
        trace?: never;
    };
    "/api/inbox/{slug}/quarantine": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The needs-review queue: mail that could not be attributed to a provider, unreviewed rows only, newest first.
         *     Owner-only. Quarantined mail is by definition mail nobody has decided who may read yet, so it cannot be shown behind per-provider access.
         */
        get: operations["listQuarantine"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/inbox/{slug}/quarantine/{messageId}/review": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Work through one needs-review row: dismiss it, or release it into a provider's inbox.
         *     A release copies the row into that provider's messages — keeping the original received_at and delete_after, so releasing does not extend retention — and marks the quarantine row reviewed, in one transaction. Owner-only, and audited either way.
         */
        post: operations["reviewQuarantineMessage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The account settings screen in one request: the caller's profile with their households, and their signed-in devices newest first. */
        get: operations["getAccountSettings"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/settings/households": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The caller's households, the same list GET /api/households/me answers with. It exists separately because the settings screen refetches it on its own after leaving a household. */
        get: operations["listSettingsHouseholds"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/settings/profile": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Update the caller's display name and avatar. */
        patch: operations["updateProfile"];
        trace?: never;
    };
    "/api/settings/sessions/others": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Sign out everywhere else: every session of this account except the one this request arrived on. */
        delete: operations["revokeOtherSessions"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/settings/sessions/{sessionId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Revoke one device. A session id that is not the caller's own revokes nothing and still answers 200 — deliberately, so the endpoint cannot be used to discover whether an id exists. */
        delete: operations["revokeSession"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/audit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The household's audit trail, newest 100 first. Owner-only: it names who did what to whom, which is exactly the sort of thing a member has no business reading about the rest of the household. */
        get: operations["listAuditEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The household's own settings: its name and the inbound address providers must be told to send codes to. */
        get: operations["getHouseholdSettings"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Rename the household. The slug — and therefore the inbound address — never changes. */
        patch: operations["updateHouseholdSettings"];
        trace?: never;
    };
    "/api/admin/{slug}/providers": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Every provider in the household with the number of sender rules that point at it, plus the rules themselves — the two halves of the provider settings screen in one request. */
        get: operations["listProviderConfigurations"];
        put?: never;
        /** Add a provider — the mailbox a set of sender rules files messages into. */
        post: operations["createProvider"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/providers/{providerId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove a provider. Its sender rules, its stored messages and every member's access to it go with it — the foreign keys cascade, which is the point: a deleted provider must not leave readable messages behind. */
        delete: operations["deleteProvider"];
        options?: never;
        head?: never;
        /** Rename a provider or change its key. */
        patch: operations["updateProvider"];
        trace?: never;
    };
    "/api/admin/{slug}/provider-rules": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Add a sender rule: the address or domain whose mail files into a provider. Exact rules beat domain rules, and the most specific domain rule wins among those. */
        post: operations["createSenderRule"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/provider-rules/{ruleId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove a sender rule. Messages already filed under its provider stay. */
        delete: operations["deleteSenderRule"];
        options?: never;
        head?: never;
        /** Repoint a sender rule, or change what it matches. */
        patch: operations["updateSenderRule"];
        trace?: never;
    };
    "/api/admin/{slug}/members": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Everybody in the household with the providers each of them may read, plus the household's providers so the screen can offer the rest. */
        get: operations["listMembers"];
        put?: never;
        /** "Add a member" — which is an invitation with no provider scope, not an account this endpoint creates. Nobody may be given a password by somebody else, so the invitee always sets their own. */
        post: operations["createMember"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/members/{userId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove somebody else from the household. Refused for the last owner, and refused for yourself — "leave household" is the route for that, so that giving up your own access is never a click away from removing somebody else's. */
        delete: operations["removeMember"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/members/{userId}/role": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Promote or demote somebody else. Refused for yourself: an owner who could demote themselves could leave a household with no owner at all, and the answer names the other owners as the way out. */
        patch: operations["updateMemberRole"];
        trace?: never;
    };
    "/api/admin/{slug}/members/{userId}/provider-access": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Let a member read one provider's messages. Idempotent: granting twice is the same as granting once, so a double-click is not an error. */
        post: operations["grantProviderAccess"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/members/{userId}/provider-access/{providerKey}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Take a provider away from a member. The key is a path parameter and only a path parameter — the TypeScript also read it from a JSON body on DELETE, which is a shape no client sends and every proxy is entitled to drop. */
        delete: operations["revokeProviderAccess"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/invitations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Every invitation this household has issued, newest first. Invitations past their expiry are marked expired before the list is read, so the screen never shows a pending invitation whose link no longer works. */
        get: operations["listInvitations"];
        put?: never;
        /** Invite somebody, optionally scoped to a set of providers they will be able to read the moment they accept. */
        post: operations["createInvitation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/invitations/{invitationId}/resend": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Issue a fresh invitation to the same person with the same role and provider scope, cancelling the old one. A new token, so the link in the first email stops working — which is what makes "resend" safe to offer after a link has been forwarded to the wrong inbox. */
        post: operations["resendInvitation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/admin/{slug}/invitations/{invitationId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Cancel a pending invitation, which makes its link stop working. */
        delete: operations["cancelInvitation"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description The acknowledgement every "it is done, and there is nothing to show for it" endpoint answers with. */
        Ok: {
            ok: boolean;
        };
        /** @description One entry in the household switcher: a household plus what the caller may do in it. */
        HouseholdSummary: {
            id: string;
            slug: string;
            displayName: string;
            /** @enum {string} */
            role: "owner" | "member";
        };
        HouseholdList: {
            households: components["schemas"]["HouseholdSummary"][];
        };
        /** @description REF §A4's `createHousehold` schema. As with SetupRequest, only the shape is stated here: the slug is normalised (trimmed, lower-cased) before REF §A3's rules are applied to it, and the message each failure produces is the wording the SPA renders next to the input. */
        CreateHouseholdRequest: {
            /** @description Trimmed, lower-cased, and subject to REF §A3's slug rules. */
            slug: string;
            /** @description Trimmed, 1..80 characters. */
            displayName: string;
        };
        /** @description A newly created household: the switcher entry's keys plus the timestamps, so the client can drop it straight into its cache. */
        HouseholdWithRole: {
            id: string;
            slug: string;
            displayName: string;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            /** @enum {string} */
            role: "owner" | "member";
        };
        HouseholdResult: {
            household: components["schemas"]["HouseholdWithRole"];
        };
        /** @description The account settings screen's view of the caller. `role` is always null: it carried Better Auth's global role in the TypeScript server, which the Go one has no counterpart for, and stays in the payload because the SPA reads it. */
        Profile: {
            id: string;
            email: string;
            name: string;
            image: string | null;
            role: string | null;
            twoFactorEnabled: boolean;
            households: components["schemas"]["HouseholdSummary"][];
        };
        /** @description One signed-in device. The session TOKEN is a bearer secret and never appears here — `isCurrent` is all the client needs to tell which row it is looking through. `ipAddress` is the digest Limen stored, not a readable address, and `impersonatedBy` is always null (the Go server has no impersonation). */
        SessionSummary: {
            id: string;
            isCurrent: boolean;
            /** Format: date-time */
            expiresAt: string;
            ipAddress: string | null;
            userAgent: string | null;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string | null;
            impersonatedBy: string | null;
        };
        AccountSettings: {
            profile: components["schemas"]["Profile"];
            sessions: components["schemas"]["SessionSummary"][];
        };
        ProfileResult: {
            profile: components["schemas"]["Profile"];
        };
        /** @description REF §A4's `profile` schema. `image` is optional and an empty string clears the avatar; anything else must be an http(s) URL of at most 2048 characters, checked in Go so the message is the TypeScript's ("image must be an http(s) URL"). */
        ProfileRequest: {
            /** @description Trimmed, 1..80 characters. */
            name: string;
            /** @description An http(s) URL, or "" to clear the avatar. */
            image?: string;
        };
        /** @description The failure envelope every error response in this API uses. `fields` carries one message per rejected input (keyed by the property or parameter name, `_` for the request body as a whole); `code` is a stable discriminator for the few failures a client must branch on rather than merely display. */
        Error: {
            error: string;
            fields?: {
                [key: string]: string;
            };
            code?: string;
        };
        /** @description REF §A2's setup/status body. `isConfigured` is always true in the Go server — internal/config requires OWNER_EMAIL and SETUP_SECRET at boot, so a running process cannot be missing them — and stays in the payload because the SPA reads it. */
        SetupStatus: {
            /** @description Configured, and setup has not been completed yet. */
            needsSetup: boolean;
            /** @description Setup is complete and this endpoint is the only thing left to say so. */
            setupLocked: boolean;
            isConfigured: boolean;
            /** @enum {string} */
            status: "pending" | "in_progress" | "complete";
            /** @description EMAIL_DOMAIN, so the client can preview "slug@domain" while naming a household. */
            emailDomain: string | null;
        };
        /**
         * @description REF §A4's `setup` schema. The shape — which properties exist, which are required, and that each is a string — is enforced here; the LENGTH and FORMAT rules named in each description below are enforced in Go (internal/api/setup.go's validateSetupBody) and deliberately not repeated as `minLength`/`pattern`.
         *     Two reasons. The values are trimmed and lower-cased before they are checked, so a schema rule would be testing what was sent rather than what is kept; and the messages the SPA renders next to each input are REF §A4's exact wording, which a schema violation cannot produce.
         */
        SetupRequest: {
            /** @description The owner's address. Trimmed and lower-cased, 3..254 characters, must look like an address. */
            email: string;
            /** @description The owner's display name. Trimmed, 1..80 characters. */
            name: string;
            /** @description 12..128 characters. Never trimmed — a space is a character the person chose. */
            password: string;
            /** @description The first household's display name. Trimmed, 1..80 characters. */
            householdName: string;
            /** @description The first household's slug. Trimmed, lower-cased, and subject to REF §A3's slug rules. */
            householdSlug: string;
            /** @description SETUP_SECRET, compared in constant time. Non-empty. */
            setupSecret: string;
        };
        /** @description REF §A4's `acceptInvitation` schema, with neither property required here: whether they are depends on the session (see the operation's requestBody comment), so the requirement, the lengths and the messages are all enforced in Go — as they are for SetupRequest above, and for the same two reasons. */
        AcceptInvitationRequest: {
            /** @description The invitee's display name. Trimmed, 1..80 characters. Required when nobody is signed in. */
            name?: string;
            /** @description 12..128 characters. Required when nobody is signed in. */
            password?: string;
        };
        /** @description The account that was just created or signed in, with its role in the household. */
        Member: {
            id: string;
            email: string;
            name: string;
            /** @enum {string} */
            role: "owner" | "member";
        };
        Household: {
            id: string;
            slug: string;
            displayName: string;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
        };
        /** @description What both "you are now the owner" (setup) and "you are now a member" (invitation accept) answer with. */
        MembershipResult: {
            member: components["schemas"]["Member"];
            household: components["schemas"]["Household"] | null;
        };
        /** @description One provider an invitation is scoped to. snake_case keys inside a camelCase parent, deliberately: it is what the TypeScript returned and what the SPA reads. */
        InvitationProvider: {
            id: string;
            provider_key: string;
            display_name: string;
        };
        /** @description One invitation record. The token itself never appears — only its SHA-256 is stored, and the plaintext lives solely in the link. */
        Invitation: {
            id: string;
            householdId: string;
            email: string;
            name: string;
            /** @enum {string} */
            role: "owner" | "member";
            /** @enum {string} */
            status: "pending" | "accepted" | "cancelled" | "expired";
            invitedByUserId: string;
            acceptedByUserId: string | null;
            /** Format: date-time */
            expiresAt: string;
            /** Format: date-time */
            acceptedAt: string | null;
            /** Format: date-time */
            cancelledAt: string | null;
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            providers: components["schemas"]["InvitationProvider"][];
        };
        /** @description Everything the invite page needs to pick a flow: create an account, sign in first, or accept as whoever is already signed in. */
        InvitationLookup: {
            invitation: components["schemas"]["Invitation"];
            household: {
                displayName: string;
            } | null;
            invitedBy: {
                name: string;
            } | null;
            /** @description An account already exists for the invited address. */
            accountExists: boolean;
            /** @description The signed-in caller, when there is one. */
            viewer: {
                email: string;
                emailMatches: boolean;
            } | null;
        };
        /** @description One line of the audit trail. `details` is whatever the action recorded (the new role, the provider key, whether an invitation email got out), parsed back from the stored JSON. */
        AuditEvent: {
            id: string;
            actorUserId: string | null;
            householdId: string | null;
            action: string;
            targetType: string;
            targetId: string | null;
            details: {
                [key: string]: unknown;
            } | null;
            /** Format: date-time */
            createdAt: string;
        };
        AuditEventList: {
            events: components["schemas"]["AuditEvent"][];
        };
        /** @description A household's own settings. `emailAddress` is `slug@EMAIL_DOMAIN` — the address providers must be told to send to — and is null only if the installation has no inbound domain configured. */
        HouseholdSettings: {
            slug: string;
            displayName: string;
            emailAddress: string | null;
        };
        HouseholdSettingsResult: {
            household: components["schemas"]["HouseholdSettings"];
        };
        /** @description REF §A4's `householdSettings` schema. Trimmed and length-checked in Go, for the wording. */
        HouseholdSettingsRequest: {
            /** @description Trimmed, 1..80 characters. */
            displayName: string;
        };
        /** @description One provider. snake_case keys, deliberately: it is what the TypeScript returned and what the SPA reads. */
        Provider: {
            id: string;
            household_id: string;
            provider_key: string;
            display_name: string;
            /** Format: date-time */
            created_at: string;
        };
        /** @description A provider plus how many sender rules point at it. */
        ProviderConfiguration: {
            id: string;
            household_id: string;
            provider_key: string;
            display_name: string;
            /** Format: date-time */
            created_at: string;
            rule_count: number;
        };
        /** @description One sender rule — the address or domain whose mail files into a provider. */
        SenderRule: {
            id: string;
            household_id: string;
            provider_id: string;
            /** @enum {string} */
            match_type: "exact" | "domain";
            match_value: string;
            /** Format: date-time */
            created_at: string;
        };
        ProviderConfigurationList: {
            providers: components["schemas"]["ProviderConfiguration"][];
            rules: components["schemas"]["SenderRule"][];
        };
        ProviderResult: {
            provider: components["schemas"]["Provider"];
        };
        /** @description REF §A4's `provider` schema. Only the shape is stated here: the key is trimmed and lower-cased before its rules run, and the message each failure produces is REF §A4's wording, which a schema violation cannot produce. */
        ProviderRequest: {
            /** @description Trimmed, lower-cased, 1..40, lowercase letters, numbers and hyphens, starting with a letter or number. */
            providerKey: string;
            /** @description Trimmed, 1..80 characters. */
            displayName: string;
        };
        SenderRuleResult: {
            rule: components["schemas"]["SenderRule"];
        };
        /** @description REF §A4's `senderRule` schema. `matchType` is a plain string here rather than an enum for the same reason the lengths are absent: the rejection must read "matchType must be exact or domain", which is checked in Go. */
        SenderRuleRequest: {
            /** @description Trimmed, 1..64 characters. */
            providerId: string;
            /** @description `exact` or `domain`. */
            matchType: string;
            /** @description Trimmed, lower-cased, 1..254. A domain rule drops any leading `@` and must then be a hostname; an exact rule must be a full email address. */
            matchValue: string;
        };
        /** @description One provider a member may read. */
        MemberProviderAccess: {
            providerKey: string;
            displayName: string;
        };
        /** @description One member of the household, with the providers they may read. `role` duplicates `householdRole`: it carried Better Auth's global role in the TypeScript server and the SPA reads both, so the Go server puts the household role in each. */
        HouseholdMember: {
            id: string;
            /** @enum {string} */
            householdRole: "owner" | "member";
            email: string;
            name: string;
            /** @enum {string} */
            role: "owner" | "member";
            /** Format: date-time */
            createdAt: string;
            /** Format: date-time */
            updatedAt: string;
            providerAccess: components["schemas"]["MemberProviderAccess"][];
        };
        MemberList: {
            members: components["schemas"]["HouseholdMember"][];
            providers: components["schemas"]["Provider"][];
        };
        /** @description REF §A4's `createMember` schema. `role` is a plain string, defaulting to member when absent: the enum would reject `admin`, which REF §A4 accepts and maps to owner, and its rejection would not read "role must be owner or member". */
        CreateMemberRequest: {
            /** @description Trimmed, lower-cased, 3..254 characters, must look like an address. */
            email: string;
            /** @description Trimmed, 1..80 characters. */
            name: string;
            /** @description `owner` or `member` (`admin` is accepted and means owner). Defaults to member. */
            role?: string;
        };
        /** @description REF §A4's `roleChange` schema. See CreateMemberRequest for why `role` is not an enum here. */
        RoleChangeRequest: {
            /** @description `owner` or `member` (`admin` is accepted and means owner). */
            role: string;
        };
        /** @description REF §A4's `providerAccess` schema. */
        ProviderAccessRequest: {
            /** @description Trimmed, lower-cased, 1..40 characters. */
            providerKey: string;
        };
        InvitationList: {
            invitations: components["schemas"]["Invitation"][];
        };
        /** @description REF §A4's `invitation` schema. See CreateMemberRequest for why `role` is a plain string. */
        InvitationRequest: {
            /** @description Trimmed, lower-cased, 3..254 characters, must look like an address. */
            email: string;
            /** @description Trimmed, 1..80 characters. */
            name: string;
            /** @description `owner` or `member` (`admin` is accepted and means owner). Defaults to member. */
            role?: string;
            /** @description The providers the invitee may read the moment they accept. At most 50; every id must belong to this household. */
            providerIds?: string[];
        };
        /** @description A newly issued invitation, its link, and whether the email carrying that link got out. A failed delivery is NOT an error (REF §A3): the invitation stands and `inviteUrl` can be shared by hand, so `emailSent` is false and `emailError` says what went wrong. */
        InvitationResult: {
            invitation: components["schemas"]["Invitation"];
            inviteUrl: string;
            emailSent: boolean;
            emailError?: string;
        };
        /**
         * @description One provider's tile on the inbox landing page: its counts plus a preview of the newest message. snake_case keys, deliberately: it is what the TypeScript returned and what the SPA reads.
         *     The `latest_*` fields are null — present and null, never missing — for a provider that has received nothing yet.
         */
        ProviderSummary: {
            household_slug: string;
            provider_key: string;
            display_name: string;
            message_count: number;
            new_count: number;
            /** Format: date-time */
            latest_received_at: string | null;
            latest_message_id: string | null;
            latest_subject: string | null;
            latest_code: string | null;
            latest_status: string | null;
        };
        ProviderSummaryList: {
            providers: components["schemas"]["ProviderSummary"][];
        };
        /** @description Which provider a page of messages belongs to. camelCase here while the rows below are snake_case, because that is the shape the SPA has read since the Workers deployment. */
        InboxProvider: {
            providerKey: string;
            displayName: string;
        };
        /** @description One message in a provider's inbox. snake_case keys, as above. */
        InboxMessage: {
            id: string;
            household_slug: string;
            provider_key: string;
            provider_display_name: string;
            subject: string | null;
            from_header: string | null;
            text_body: string;
            extracted_code: string | null;
            /** @enum {string} */
            status: "new" | "used" | "expired";
            /** Format: date-time */
            received_at: string;
        };
        /** @description One row of the needs-review queue. `provider_key` and `provider_display_name` are the literals "quarantine" and "Quarantine" so the SPA can render it with the same component as an inbox row, and `envelope_from` is included because who the mail server said sent it is the main thing an owner reviews. */
        QuarantineMessage: {
            id: string;
            household_slug: string;
            provider_key: string;
            provider_display_name: string;
            subject: string | null;
            from_header: string | null;
            envelope_from: string;
            text_body: string;
            extracted_code: string | null;
            status: string;
            quarantine_reason: string;
            /** Format: date-time */
            received_at: string;
        };
        /**
         * @description The keyset page the server actually served: the limit it applied (after clamping) and the cursor for the next, older page.
         *     `nextBefore` is null when this was the last page, which is what the SPA checks to decide whether to offer "load more".
         */
        PageInfo: {
            limit: number;
            /** Format: date-time */
            nextBefore: string | null;
        };
        InboxMessagePage: {
            provider: components["schemas"]["InboxProvider"];
            messages: components["schemas"]["InboxMessage"][];
            page: components["schemas"]["PageInfo"];
        };
        QuarantineMessagePage: {
            messages: components["schemas"]["QuarantineMessage"][];
            page: components["schemas"]["PageInfo"];
        };
        InboxMessageResult: {
            message: components["schemas"]["InboxMessage"];
        };
        /** @description REF §A4's `messageStatus` schema. `status` is a plain string rather than an enum for the same reason `matchType` is (see SenderRuleRequest): the rejection must read "status must be new, used or expired", and an enum violation cannot say that. */
        MessageStatusRequest: {
            /** @description `new`, `used` or `expired`. */
            status: string;
        };
        /** @description REF §A4's `quarantineReview` schema. `action` is a plain string for the reason MessageStatusRequest's `status` is; `providerKey` is required in practice for a release, but the 400 that says so is the handler's, because it names the field. */
        QuarantineReviewRequest: {
            /** @description `dismiss` or `release`. */
            action: string;
            /** @description The provider to release into. Trimmed, lower-cased, 1..40. Required for a release, ignored for a dismissal. */
            providerKey?: string;
        };
        /** @description The outcome of one review. `releasedMessage` is the stored copy for a release and null for a dismissal — present and null, never missing. */
        QuarantineReviewResult: {
            /** Format: date-time */
            reviewedAt: string;
            releasedMessage: components["schemas"]["InboxMessage"] | null;
        };
    };
    responses: never;
    parameters: {
        /**
         * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
         *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
         */
        HouseholdSlug: string;
        /**
         * @description The invitation token from the link. A header rather than a path or query parameter so the secret never lands in a request log, a browser history entry or a Referer.
         *     Declared optional, and with no length bounds, so that EVERY way of getting the token wrong is answered by the handler rather than by a schema violation: a missing or empty header with REF §A2's "Invitation token header is required", and a token of any length that does not match a stored hash with the same 404 as a wrong one. A `minLength` here would turn an empty header into a validation message, and a `maxLength` would tell a guesser that their token was at least the right size. It is mandatory in practice.
         */
        InvitationToken: string;
        /** @description A provider's id. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
        ProviderId: string;
        /** @description A sender rule's id, scoped to the household in the path. */
        SenderRuleId: string;
        /** @description The member's user id. Membership of the household in the path is checked before anything is done with it. */
        MemberUserId: string;
        /** @description An invitation's id, scoped to the household in the path. */
        InvitationId: string;
        /** @description A message's id — an inbox message for the status route, a quarantined one for the review route. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
        MessageId: string;
        /**
         * @description How many rows to return. Clamped to 1..200 in Go, with 50 for a missing value.
         *     Deliberately declared WITHOUT `minimum`/`maximum`: the TypeScript clamped an out-of-range limit rather than rejecting it, and a client that asks for 9999 should get 200 rows, not a validation error on a page it can no longer navigate away from.
         *     The type IS stated, which is the one place this diverges from that predecessor: `limit=abc` is a 400 from request validation here, where the TypeScript's `Number(...)` turned it into NaN and fell back to the default. Saying "integer" is worth that — a client sending a non-number is broken, and every value a working client sends is clamped rather than refused.
         */
        PageLimit: number;
        /**
         * @description The keyset cursor: return only rows older than this instant, which is the previous page's `nextBefore`.
         *     Declared as a plain string rather than `format: date-time` for the same reason `limit` has no bounds: a cursor that does not parse is IGNORED (the newest page is returned) rather than rejected, exactly as the TypeScript's normalizePageOptions did.
         */
        PageBefore: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    healthz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The process is alive. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @enum {boolean} */
                        ok: true;
                    };
                };
            };
        };
    };
    readyz: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The process is ready to serve traffic. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @enum {boolean} */
                        ok: true;
                        status: string;
                        setupConfigured: boolean;
                        retention: {
                            /**
                             * Format: date-time
                             * @description When the retention job last completed, or null if it never has.
                             */
                            lastRunAt: string | null;
                            /** @description Whether the last run is older than the tolerated window (48 hours), or has never happened. */
                            stale: boolean;
                        };
                    };
                };
            };
            /** @description The database is unreachable. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @enum {boolean} */
                        ok: false;
                        error: string;
                    };
                };
            };
        };
    };
    getSetupStatus: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The installation's first-run state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SetupStatus"];
                };
            };
        };
    };
    completeSetup: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetupRequest"];
            };
        };
        responses: {
            /** @description Setup completed. The response also carries the owner's session cookie, so the SPA lands signed in. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MembershipResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Refused. See the envelope's `error` for which check failed. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The request conflicts with the installation's current state. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Rate limited. `Retry-After` says when to come back. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The request could not be completed. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    lookupInvitation: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description The invitation token from the link. A header rather than a path or query parameter so the secret never lands in a request log, a browser history entry or a Referer.
                 *     Declared optional, and with no length bounds, so that EVERY way of getting the token wrong is answered by the handler rather than by a schema violation: a missing or empty header with REF §A2's "Invitation token header is required", and a token of any length that does not match a stored hash with the same 404 as a wrong one. A `minLength` here would turn an empty header into a validation message, and a `maxLength` would tell a guesser that their token was at least the right size. It is mandatory in practice.
                 */
                "X-Invitation-Token"?: components["parameters"]["InvitationToken"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The invitation, who sent it, and how the viewer relates to it. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationLookup"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such invitation, or it is no longer valid. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The invitation has expired. */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Rate limited. `Retry-After` says when to come back. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    acceptInvitation: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description The invitation token from the link. A header rather than a path or query parameter so the secret never lands in a request log, a browser history entry or a Referer.
                 *     Declared optional, and with no length bounds, so that EVERY way of getting the token wrong is answered by the handler rather than by a schema violation: a missing or empty header with REF §A2's "Invitation token header is required", and a token of any length that does not match a stored hash with the same 404 as a wrong one. A `minLength` here would turn an empty header into a validation message, and a `maxLength` would tell a guesser that their token was at least the right size. It is mandatory in practice.
                 */
                "X-Invitation-Token"?: components["parameters"]["InvitationToken"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["AcceptInvitationRequest"];
            };
        };
        responses: {
            /** @description Accepted by the signed-in user. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MembershipResult"];
                };
            };
            /** @description The invited account was created and the invitation accepted. The response also carries the new session cookie. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MembershipResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Refused. See the envelope's `error` for which check failed. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such invitation, or it is no longer valid. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The request conflicts with the installation's current state. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The invitation has expired. */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Rate limited. `Retry-After` says when to come back. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The request could not be completed. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listMyHouseholds: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's households. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HouseholdList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    createHousehold: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateHouseholdRequest"];
            };
        };
        responses: {
            /** @description The household, in the same shape as a /api/households/me entry so the client can use it without a refetch. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HouseholdResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller may not create a household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The slug is taken. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Rate limited. `Retry-After` says when to come back. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    leaveHousehold: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The membership is gone; provider access cascaded with it. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not a member of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is the household's only owner. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listInboxProviders: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The providers this caller may read. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProviderSummaryList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not a member of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listProviderMessages: {
        parameters: {
            query?: {
                /**
                 * @description How many rows to return. Clamped to 1..200 in Go, with 50 for a missing value.
                 *     Deliberately declared WITHOUT `minimum`/`maximum`: the TypeScript clamped an out-of-range limit rather than rejecting it, and a client that asks for 9999 should get 200 rows, not a validation error on a page it can no longer navigate away from.
                 *     The type IS stated, which is the one place this diverges from that predecessor: `limit=abc` is a 400 from request validation here, where the TypeScript's `Number(...)` turned it into NaN and fell back to the default. Saying "integer" is worth that — a client sending a non-number is broken, and every value a working client sends is clamped rather than refused.
                 */
                limit?: components["parameters"]["PageLimit"];
                /**
                 * @description The keyset cursor: return only rows older than this instant, which is the previous page's `nextBefore`.
                 *     Declared as a plain string rather than `format: date-time` for the same reason `limit` has no bounds: a cursor that does not parse is IGNORED (the newest page is returned) rather than rejected, exactly as the TypeScript's normalizePageOptions did.
                 */
                before?: components["parameters"]["PageBefore"];
            };
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description The provider's key, scoped to the household in the path. Bounded the way `revokeProviderAccess` bounds its own copy (REF §A4's `providerAccess` rules), because a path parameter has no `fields` entry to render a Go-side message against. */
                providerKey: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The page of messages, with the cursor for the next one. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InboxMessagePage"];
                };
            };
            /** @description The provider key or a page parameter is not of the stated shape. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not a member of this household, or is a member without access to this provider. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description This household has no such provider. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateMessageStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A message's id — an inbox message for the status route, a quarantined one for the review route. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
                messageId: components["parameters"]["MessageId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MessageStatusRequest"];
            };
        };
        responses: {
            /** @description The message as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InboxMessageResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not a member of this household, or is a member without access to the message's provider. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description This household has no such message. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listQuarantine: {
        parameters: {
            query?: {
                /**
                 * @description How many rows to return. Clamped to 1..200 in Go, with 50 for a missing value.
                 *     Deliberately declared WITHOUT `minimum`/`maximum`: the TypeScript clamped an out-of-range limit rather than rejecting it, and a client that asks for 9999 should get 200 rows, not a validation error on a page it can no longer navigate away from.
                 *     The type IS stated, which is the one place this diverges from that predecessor: `limit=abc` is a 400 from request validation here, where the TypeScript's `Number(...)` turned it into NaN and fell back to the default. Saying "integer" is worth that — a client sending a non-number is broken, and every value a working client sends is clamped rather than refused.
                 */
                limit?: components["parameters"]["PageLimit"];
                /**
                 * @description The keyset cursor: return only rows older than this instant, which is the previous page's `nextBefore`.
                 *     Declared as a plain string rather than `format: date-time` for the same reason `limit` has no bounds: a cursor that does not parse is IGNORED (the newest page is returned) rather than rejected, exactly as the TypeScript's normalizePageOptions did.
                 */
                before?: components["parameters"]["PageBefore"];
            };
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The page of quarantined messages, with the cursor for the next one. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QuarantineMessagePage"];
                };
            };
            /** @description A page parameter is not of the stated shape. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    reviewQuarantineMessage: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A message's id — an inbox message for the status route, a quarantined one for the review route. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
                messageId: components["parameters"]["MessageId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["QuarantineReviewRequest"];
            };
        };
        responses: {
            /** @description The review. `releasedMessage` is the stored copy for a release and null for a dismissal. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QuarantineReviewResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description This household has no such provider, or no such unreviewed quarantine message. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    getAccountSettings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The profile and the session list. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountSettings"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The session's account no longer exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listSettingsHouseholds: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's households. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HouseholdList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateProfile: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProfileRequest"];
            };
        };
        responses: {
            /** @description The profile as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProfileResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The session's account no longer exists. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    revokeOtherSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The other sessions are gone. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    revokeSession: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session is gone, if it was the caller's. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listAuditEvents: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The trail. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEventList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    getHouseholdSettings: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The settings. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HouseholdSettingsResult"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The household is gone. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateHouseholdSettings: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["HouseholdSettingsRequest"];
            };
        };
        responses: {
            /** @description The settings, as they now stand. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HouseholdSettingsResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The household is gone. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listProviderConfigurations: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The providers and their sender rules. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProviderConfigurationList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    createProvider: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProviderRequest"];
            };
        };
        responses: {
            /** @description The provider. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProviderResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description A provider with that key already exists in this household. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    deleteProvider: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A provider's id. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
                providerId: components["parameters"]["ProviderId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The provider and everything filed under it are gone. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateProvider: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A provider's id. Scoped to the household in the path, so an id from another household is a 404 rather than a leak. */
                providerId: components["parameters"]["ProviderId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProviderRequest"];
            };
        };
        responses: {
            /** @description The provider, as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProviderResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description Another provider in this household already has that key. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    createSenderRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SenderRuleRequest"];
            };
        };
        responses: {
            /** @description The rule. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SenderRuleResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description This household already has a rule for that match type and value. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    deleteSenderRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A sender rule's id, scoped to the household in the path. */
                ruleId: components["parameters"]["SenderRuleId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The rule is gone. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such rule in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateSenderRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description A sender rule's id, scoped to the household in the path. */
                ruleId: components["parameters"]["SenderRuleId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SenderRuleRequest"];
            };
        };
        responses: {
            /** @description The rule, as it now stands. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SenderRuleResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider, or no such rule, in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description This household already has a rule for that match type and value. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listMembers: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The members and the household's providers. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MemberList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    createMember: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateMemberRequest"];
            };
        };
        responses: {
            /** @description The invitation, its link, and whether the email carrying that link was delivered. A failed delivery is not an error (REF §A3) — the owner can share `inviteUrl` by hand. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationResult"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    removeMember: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description The member's user id. Membership of the household in the path is checked before anything is done with it. */
                userId: components["parameters"]["MemberUserId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The membership is gone; provider access cascaded with it. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description The caller asked to remove themselves. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description That user is not a member of this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description That member is the household's only owner. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    updateMemberRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description The member's user id. Membership of the household in the path is checked before anything is done with it. */
                userId: components["parameters"]["MemberUserId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RoleChangeRequest"];
            };
        };
        responses: {
            /** @description The role is changed. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner, or asked to change their own role. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description That user is not a member of this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    grantProviderAccess: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description The member's user id. Membership of the household in the path is checked before anything is done with it. */
                userId: components["parameters"]["MemberUserId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProviderAccessRequest"];
            };
        };
        responses: {
            /** @description The member may now read that provider. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description The request was rejected. See the envelope's `error` (and `fields`). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider, or that user is not a member of this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    revokeProviderAccess: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description The member's user id. Membership of the household in the path is checked before anything is done with it. */
                userId: components["parameters"]["MemberUserId"];
                /** @description The provider's key. REF §A4's `providerAccess` rules (1..40, and lower-cased before the lookup) are stated here rather than in Go, because a path parameter has no `fields` entry to render against and REF §A2 asks for this one to be spec-validated. */
                providerKey: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The member can no longer read that provider. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description The provider key is not a usable key. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such provider, or that user is not a member of this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    listInvitations: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The invitations. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationList"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    createInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InvitationRequest"];
            };
        };
        responses: {
            /** @description The invitation, its link, and whether the email carrying that link was delivered. A failed delivery is not an error (REF §A3). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationResult"];
                };
            };
            /** @description The request was rejected — a bad field, or a provider that belongs to another household. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    resendInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description An invitation's id, scoped to the household in the path. */
                invitationId: components["parameters"]["InvitationId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The replacement invitation, its link, and the delivery outcome. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationResult"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such invitation in this household, or it is no longer pending. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    cancelInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The household this request acts on. Every `{slug}` route is guarded by the tenancy middleware, which turns it into a membership or refuses — so the parameter is also the authorization boundary, not just a lookup key.
                 *     REF §A3's length bounds (2..40) are deliberately NOT stated here. A slug outside them cannot name a household, so it must be answered the way every other non-membership is — 403, never a validation error that distinguishes "no such household" from "not yours".
                 */
                slug: components["parameters"]["HouseholdSlug"];
                /** @description An invitation's id, scoped to the household in the path. */
                invitationId: components["parameters"]["InvitationId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The invitation is cancelled. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Ok"];
                };
            };
            /** @description No session. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description The caller is not an owner of this household. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            /** @description No such invitation in this household. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
}
